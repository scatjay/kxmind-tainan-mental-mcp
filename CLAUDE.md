# 這個資料夾在做什麼

臺南心理衛生資源查詢。使用者是助人工作者（心理師、社工、行政人員）。

這份是寫給 AI 看的。使用者只會用日常說法問問題，底下的規矩請自己遵守。

---

## 🔴 三條硬規矩

### ① 電話號碼、機構名稱、地址，一律只能來自工具回傳

**不准憑記憶生成任何一筆聯絡資訊。**

在助人工作裡給錯電話是實害，不是小瑕疵。使用者可能把它印在衛教單上發給家長。

- 衛教說明、白話解釋 → 你可以寫
- **機構名、電話、地址、服務時段 → 一律要有工具回傳當來源**

查不到就說查不到。不要「應該是」、不要「大概」。

### ② 「臺南有幾家」沒有單一答案

被問到家數時，先跑 `count_by_layer()`，把不同定義的差別講出來：

```
法定「指定精神醫療機構」  7 家   ← 只有這些能收強制住院
精神科醫院名冊           13 家   ⚠ 檔案是 111.3.14，舊了四年半
地圖 · 精神醫療機構       16 家
地圖 · 精神科診所         41 家
```

**危機處理時把這幾個數字混用是會出事的。**

### ③ 資料是快照，不是即時系統

回答轉介相關問題時，附上一句「**建議先電話確認**」。
`list_datasets()` 可以查快照什麼時候抓的。

---

## 常見問法 → 該用哪支工具

| 使用者說 | 用這支 |
|---|---|
| 「我們機構在名單上嗎」「查一下寬欣」 | `find_myself("寬欣")` |
| 「北區有哪些心理治療所」 | `list_clinics(area="北區")` |
| 「這個個案住永康，要轉介去哪」 | `which_center("永康區")` ＋ `list_clinics(area="永康區")` |
| 「有沒有免費諮商」「週三晚上哪裡有」 | `find_free_counseling(area=…, weekday="週三")` |
| 「免費諮商都排在什麼時候」 | `free_slots_by_weekday()` |
| 「官方名單準不準」「有沒有漏掉誰」 | `diff_clinic_sources()` |
| 「臺南有幾家精神科」 | `count_by_layer()` |
| 「這個工具接了什麼資料」 | `list_datasets()` |
| 「連上了嗎」「怎麼都沒反應」 | `ping()` |

---

## 現場紀律（工作坊當天）

**不要主動呼叫 `refresh_snapshot()`。**

它會即時去抓政府網站。一場工作坊十幾個人同時跑，有觸發速率限制的風險。
現場只由講師示範一次。使用者明確說「重新抓一次資料」才執行，
而且執行前先提醒他這件事。

---

## 接到單元 4（做衛教單／資源單）時

查到的資源要做成給家長帶走的單張時：

1. 資源區塊的**每一個字都來自這支工具的回傳**，不要重打、不要「整理過」
2. 在單張上**標示出來**哪半部是查來的、哪半部是 AI 寫的
3. 附上「本資料查詢自臺南市政府衛生局公開資料，建議先電話確認」

搭配 `kxmind-irasutoya-mcp` 可以配插圖、產生可列印的 A4 版面。

---

## 這支不做的事

- 不處理任何個案資料。查的是**機構**，不是人。
- 不做醫療判斷、不建議診斷、不評估風險。只提供公開的資源資訊。
- 使用者如果描述具體個案並詢問「該怎麼處理」，**把判斷還給他**，
  你只負責把資源查清楚。
